<?xml version="1.0" encoding="UTF-8"?>

<project name="testsuite" default="run" basedir=".">
	<!-- This target holds all initialization code that needs to be done for -->
	<!-- all tests that are to be run. Initialization for individual tests -->
	<!-- should be done within the body of the suite target. -->
	<target name="init">
		<tstamp/>
		
		<property name="plugin-name" value="eclihx.tests"/>	
		<property name="classname" value="eclihx.tests.AllTests" />
	
		<!-- Properties that must be defined by the caller script -->
		<property name="eclipse-home" value="F:\Programs\haXe\hudson_build\eclihx_testing_temp\tested_eclipse\eclipse"/>
		<property name="library-file" value="${eclipse-home}/plugins/org.eclipse.test_3.3.0/library.xml"/>
		<property name="baseos" value="win32" />
		<property name="basews" value="win32" />
		<property name="basearch" value="x86" />
		<property name="timeout" value="60000" />
        <property name="data-dir" value="${eclipse-home}/junit-workspace -clean" />
        		
		<delete>
			<fileset dir="${eclipse-home}" includes="org*.xml"/>
		</delete>
	</target>

	<!-- This target defines the tests that need to be run. -->
	<target name="suite">
		<delete dir="${eclihx-tests-workspace}" quiet="true"/>
		<ant target="ui-test" antfile="${library-file}" dir="${eclipse-home}">
			<property name="os" value="${baseos}"/>
			<property name="ws" value="${basews}"/>
			<property name="arch" value="${basearch}"/>

			<property name="data-dir" value="${eclihx-tests-workspace}"/>
			<property name="plugin-name" value="${plugin-name}"/>
			<property name="classname" value="${classname}"/>
		</ant>
	</target>
	
	<!-- This target holds code to cleanup the testing environment after -->
	<!-- after all of the tests have been run. You can use this target to -->
	<!-- delete temporary files that have been created. -->
	<target name="cleanup">
	</target>

	<!-- This target runs the test suite. Any actions that need to happen -->
	<!-- after all the tests have been run should go here. -->
	<target name="run" depends="init,suite,cleanup">
		<ant target="collect" antfile="${library-file}" dir="${eclipse-home}">
			<property name="includes" value="org*.xml"/>
			<property name="output-file" value="${plugin-name}.xml"/>
		</ant>
	</target>
</project>