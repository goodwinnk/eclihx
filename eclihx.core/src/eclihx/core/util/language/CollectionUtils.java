package eclihx.core.util.language;

/**
 * Utility class for simplifying working with collections.
 * Let's try some more fp-style in java.
 */
public class CollectionUtils
{
	/**
	 * Method for array creation with the derived type.
	 * 
	 * @param <T> array element type
	 * @param tArray array elements
	 * @return new array with the given elements.
	 */
	public static <T> T[] array(T... tArray) {
		return tArray;
	}
}

